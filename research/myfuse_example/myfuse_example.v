#!/usr/bin/env -S v -gc none -no-retry-compilation -cc tcc -d use_openssl -enable-globals run

import myfuse

fn main() {

    //TODO: do this as fixed arguments in the script, mount on /tmp/mymount

}